class Solution {
public:
    int romanToInt(string s) {
        int count = s.size();
        int result = 0;
        char val1 = "I";
        for (int i = 0; i < count; i++)
        {
            if (s.at(i) == val1)
                resilt +=1;
        }
    }
};