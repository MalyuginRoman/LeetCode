class Solution {
public:
    int romanToInt(string s) {
        int count = s.size();
        int result = 0;
        for (int i = 0; i < count; i++)
        {
            if (s.at(i) == "I")
                resilt +=1;
        }
    }
};